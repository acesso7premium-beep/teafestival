version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./teafestival.db:/app/teafestival.db
    environment:
      - DATABASE_URL=sqlite:///./teafestival.db
    command: python main.py
